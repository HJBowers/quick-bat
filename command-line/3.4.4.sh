# In Listing 18, we saw two additional lines that case-insensitively matched
# “rose”. Execute a command confirming that both of the lines contain the
# string “Rose” (and not, e.g., “rOSe”). Hint: Use a case-sensitive grep for
# “Rose”.

grep Rose sonnets.txt
