# The history command prints the history of commands in a particular terminal
# shell (subject to some limit, which is typically large). Pipe history to less
# to examine your command history. What was your 17th command?

history | less
#git branch
