# What is the command to list all the non-hidden files that contain the string
# “onnet”, long-form by reverse modification time? Hint: Use the wildcard
# operator at both the beginning and the end.

ls *'onnet'*
