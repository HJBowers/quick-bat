# In Box 9, we learned about !! (“bang bang”) to execute the previous command.
# Similarly, !n executes command number n, so that, e.g., !17 executes the
# 17th command in the command history. Use the result from the previous
# exercise to re-run the last occurrence of curl.

!293
#histroy | grep curl
